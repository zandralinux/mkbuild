
fetch:QV: fetch-git fetch-http

fetch-git:QV:
	if test x"$git" = x""; then
		return
	fi
	if test x"$branch" = x""; then
		branch=master
	fi
	test -d $src || git clone -b $branch $git $src

fetch-http:QV:
	if test x"$url" = x""; then
		return
	fi
	wget -c $url
	tar -xf `basename $url`
